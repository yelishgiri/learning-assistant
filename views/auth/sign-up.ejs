<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <title>Sign Up</title>
    <link rel="stylesheet" href="/auth/sign-up/styles.css" />
  </head>
  <body>
    <div class="container">
      <div class="logo">
        <h1>Studyo</h1>
        <p>Welcome to your Integrated Study Environment</p>
      </div>
      <h1>Create Your Account</h1>
      <form action="/auth/sign-up" method="POST">
        <div class="form-group">
          <label for="user_username">Username</label>
          <input
            id="user_username"
            name="user_username"
            placeholder="john.doe1"
            type="text"
            required
          />
        </div>

        <div class="form-group">
          <label for="user_email">Email Address</label>
          <input
            id="user_email"
            name="user_email"
            placeholder="johndoe@gmail.com"
            type="email"
            required
          />
        </div>

        <div class="form-group">
          <label for="user_first_name">First Name</label>
          <input
            id="user_first_name"
            name="user_first_name"
            placeholder="John"
            type="text"
            required
          />
        </div>

        <div class="form-group">
          <label for="user_last_name">Last Name</label>
          <input
            id="user_last_name"
            name="user_last_name"
            placeholder="Doe"
            type="text"
            required
          />
        </div>

        <div class="form-group">
          <label for="user_password">Password</label>
          <input
            id="user_password"
            name="user_password"
            type="password"
            placeholder="********"
            required
          />
        </div>

        <div class="form-group">
          <label for="user_confirm_password">Confirm Password</label>
          <input
            id="user_confirm_password"
            name="user_confirm_password"
            placeholder="********"
            type="password"
            required
          />
        </div>

        <button type="submit">Create Account</button>
      </form>

      <div class="login-link">
        Already have an account? <a href="/auth/log-in">Sign In</a>
      </div>
    </div>
  </body>
</html>
